services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - PUBLIC_URL=${PUBLIC_URL:-https://your-domain.com}
    volumes:
      - happy-data:/app/.happy

volumes:
  happy-data:
